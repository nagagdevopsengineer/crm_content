'use strict';

/**
 * driverbulkuploadlog router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::driverbulkuploadlog.driverbulkuploadlog');
