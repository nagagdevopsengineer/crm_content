'use strict';

/**
 * driverbulkuploadlog service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::driverbulkuploadlog.driverbulkuploadlog');
