apiVersion: v1
kind: Service
metadata:
  name: poc-env-crm-content
  namespace: poc-env-crm-content
  labels:
    app: poc-env-crm-content
spec:
  type: LoadBalancer
  ports:
  - name: https
    port: 1337
    targetPort: 1337
  selector:
    app: poc-env-crm-content

