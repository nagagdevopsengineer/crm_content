apiVersion: v1
kind: Service
metadata:
  name: crm-content
  namespace: crm-content
  labels:
    app: crm-content
spec:
  # type: LoadBalancer
  # ports:
  # - name: https
  # port: 1337
  # targetPort: 1337
  type: ExternalName
  ports:
  - name: https
  port: 1337
  targetPort: 1337
  externalName: dev.arrivnow.com
  selector:
    app: crm-content
